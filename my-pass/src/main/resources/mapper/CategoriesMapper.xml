<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.mapper.CategoriesMapper">

    <!-- 查询用户所有分类，按排序升序 -->
    <select id="selectByUserIdOrderBySort" resultType="org.example.entity.Categories">
        SELECT id, user_id, name, sort_order, created_at
        FROM categories
        WHERE user_id = #{userId}
        ORDER BY sort_order ASC
    </select>

    <!-- 查询最大排序值 -->
    <select id="selectMaxSortOrderByUserId" resultType="java.lang.Integer">
        SELECT MAX(sort_order)
        FROM categories
        WHERE user_id = #{userId}
    </select>

    <!-- 批量更新排序（使用 CASE WHEN） -->
    <update id="batchUpdateSortOrder" parameterType="java.util.List">
        UPDATE categories
        SET sort_order = CASE id
        <foreach collection="list" item="item">
            WHEN #{item.id} THEN #{item.sortOrder}
        </foreach>
        END
        WHERE id IN
        <foreach collection="list" item="item" open="(" separator="," close=")">
            #{item.id}
        </foreach>
    </update>

    <select id="hasName" resultType="boolean">
        SELECT COUNT(*) > 0
        FROM categories
        WHERE user_id = #{userId} AND name = #{name}
    </select>
</mapper>