<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.mapper.UsersMapper">
    <!-- 重写 insert，不插入时间字段，让数据库自动生成 -->
    <insert id="insert" parameterType="org.example.entity.Users" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (user_name, password_hash)
        VALUES (#{userName}, #{passwordHash})
        <!-- 注意：不包含 created_at 和 updated_at -->
    </insert>

    <!-- 根据用户名查询用户 -->
    <select id="findByUserName" resultType="org.example.entity.Users">
        SELECT
        id,
        user_name,
        password_hash as passwordHash,
        created_at as createdAt,
        updated_at as updatedAt
        FROM users
        WHERE user_name = #{userName}
    </select>



    <!-- 高级查询示例：分页 + 模糊搜索 -->
    <select id="searchUsers" resultType="org.example.entity.Users">
        SELECT *
        FROM users
        <where>
            <if test="keyword != null and keyword != ''">
                AND username LIKE CONCAT('%', #{keyword}, '%')
            </if>
            <if test="startTime != null">
                AND created_at >= #{startTime}
            </if>
        </where>
        ORDER BY created_at DESC
    </select>
</mapper>