<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.mapper.PlatformsMapper">

    <!-- 查询平台列表 -->
    <select id="selectByUserIdAndCategory" resultType="org.example.entity.Platforms">
        SELECT id, user_id, category_id, name, url, sort_order, created_at, updated_at
        FROM platforms
        WHERE user_id = #{userId}
        <if test="categoryId != null">
            AND category_id = #{categoryId}
        </if>
        ORDER BY sort_order ASC
    </select>

    <!-- 查询最大排序值 -->
    <select id="selectMaxSortOrderByUserId" resultType="java.lang.Integer">
        SELECT MAX(sort_order)
        FROM platforms
        WHERE user_id = #{userId}
    </select>

    <!-- 批量更新排序 -->
    <update id="batchUpdateSortOrder">
        UPDATE platforms
        SET sort_order = CASE id
        <foreach collection="list" item="item">
            WHEN #{item.id} THEN #{item.sortOrder}
        </foreach>
        END
        WHERE id IN
        <foreach collection="list" item="item" open="(" separator="," close=")">
            #{item.id}
        </foreach>
    </update>

</mapper>